$bossbar add warden.bossbar.$(bossbar_id) "Warden"
$bossbar set warden.bossbar.$(bossbar_id) style progress
$bossbar set warden.bossbar.$(bossbar_id) color blue
$bossbar set warden.bossbar.$(bossbar_id) max 500
